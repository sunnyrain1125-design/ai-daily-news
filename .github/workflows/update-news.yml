name: Update AI Daily News

on:
  schedule:
    - cron: "*/5 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update-news:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          version: 10

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Refresh AI daily news
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
          GEMINI_MODEL: gemini-2.5-flash
          NEWS_TIMEZONE: Asia/Taipei
          NEWS_CRON: "*/5 * * * *"
          NEWS_MAX_STORY_AGE_DAYS: 7
          NEWS_MIN_VISIBLE_STORIES: 8
          CRON_SECRET: ${{ secrets.CRON_SECRET }}
        run: pnpm refresh

      - name: Commit updated data
        run: |
          if git diff --quiet -- public/data.json public/history; then
            echo "No data changes to commit."
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add public/data.json public/history
          git commit -m "chore: update AI daily news"
          git push
